<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FreeBird" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="statusBarBackground">@color/black</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="statusBarScrim">@color/white</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:listDivider">@color/black</item>
    </style>
</resources>