<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FreeBird" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->

        <item name="android:statusBarColor">@color/black</item>

        <item name="statusBarBackground">@color/black</item>
        <item name="statusBarForeground">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="statusBarScrim">@color/black</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:listDivider">@color/black</item>
    </style>

    <style name="Theme.FreeBird" parent="Base.Theme.FreeBird" />
</resources>